# Minis Ultra 1.36.46-linux

versionCode **99**。包名 `com.openminis.linux`。产品名仍是 Minis Ultra。

## 本版

- 不折叠时，翻译写回按去掉首尾空白后的正文对齐。段尾换行不再让这一段的译文写不回数据库。
- 两段可见文字相同（只是换行不同）时，仍按这一段在回复里的次序落盘。后一段不会改掉前一段。
- 带系统提醒的段落仍留下提醒本身。工具卡片不参与这次写回。
