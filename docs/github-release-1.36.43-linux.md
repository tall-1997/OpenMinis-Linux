# Minis Ultra 1.36.43-linux

versionCode **96**。包名 `com.openminis.linux`。产品名仍是 Minis Ultra。

## 本版

- 折叠思考和工具过程时，任务结束后只剩最后一个翻译按钮。它翻译的是整段可见回复，不再只翻译最后一小段。
- 写回时工具卡片还在。前面的正文不会在重新打开会话后又出现在译文上面。带系统提醒的段落会留下提醒本身。
- 不折叠时，每个正文段仍只翻译自己那一段。相同文字的后一段不会被前一段的写回一起改掉。
- `cronjob` 创建若命中已有的相同任务，返回的是已经落盘的 id，不再报一个并不存在的新 id。
- `cronjob` 删除接受唯一 id 前缀，删完会再读一次确认。前缀对上多条时会列出它们，不会静默失败。
